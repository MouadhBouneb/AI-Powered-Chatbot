# ============================================================================
# AI-Powered Chatbot - Environment Variables
# ============================================================================
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp env.example .env
# 2. Update JWT_SECRET with a secure random string
# 3. Adjust other values if needed (defaults work for local development)
# ============================================================================

# --------------------------------------
# REQUIRED: Security Configuration
# --------------------------------------

# JWT Secret Key (REQUIRED - CHANGE THIS!)
# This is used to sign authentication tokens
# Generate a secure one: openssl rand -base64 32
# Or use any random 32+ character string
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# --------------------------------------
# Backend Configuration
# --------------------------------------

# CORS Origin - Frontend URL that can access the API
# Default for local development (don't change unless using different port)
CORS_ORIGIN=http://localhost:3000

# Ollama AI Base URL - Where the AI models are running
# Default: http://ollama:11434 (Docker internal network)
# For external Ollama: http://localhost:11434 or http://your-ollama-server:11434
# Note: Docker Compose sets this automatically, only change if using external Ollama
OLLAMA_BASE_URL=http://ollama:11434

# Ollama AI - Number of CPU threads for AI model processing
# Recommended: Set to your CPU core count (check Task Manager)
# 4 cores = 4, 8 cores = 8, 16+ cores = 16
OLLAMA_NUM_THREADS=8

# --------------------------------------
# Frontend Configuration
# --------------------------------------

# Backend API URL - Where frontend sends requests
# Default for local development (don't change unless using different port)
VITE_API_URL=http://localhost:4000/api

# ============================================================================
# That's it! Run: ./run.sh (Linux/Mac) or run.bat (Windows)
# ============================================================================